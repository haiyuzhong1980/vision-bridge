{
  "name": "vision-bridge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cross-channel OpenClaw plugin for deterministic image understanding.",
  "scripts": {
    "smoke": "node --experimental-strip-types ./smoke-test.ts",
    "test": "node --experimental-strip-types --test tests/*.ts"
  },
  "engines": {
    "node": ">=22"
  },
  "openclaw": {
    "extensions": [
      "./index.ts"
    ]
  }
}
